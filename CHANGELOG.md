# 변경 내역

릴리스 워크플로가 `## <버전>` 구간을 그대로 뽑아 CurseForge 변경 내역과 GitHub Release 본문으로 씁니다.
버전을 올릴 때 **맨 위에** 새 구간을 추가하세요.

## 0.3.0

**마을이 스스로 짓지 않는다.** 이 모드는 이제 플레이어가 할 수 없는 것만 준다 — 0.2를 실제로 플레이해서 나온 결론이고, 근거는 `docs/concept-0.3.md`.

**0.2.x 월드와 호환되지 않는다.** 마을 데이터·영주의 탁자·영주의 인장·골렘의 심장이 전부 사라진다. 0.2가 지은 블록은 그대로 서 있다. 새 월드를 권한다.

### 새로 생긴 것

- **도면과 제도판** — 건축가의 탁자에 도면을 꽂으면 그 옆에 건물이 그려진다. 상대 좌표·회전·윤곽 켜기/끄기, 그리고 짓기. **타이머가 없다** — 막는 블록을 캐러 갔다 와도 그대로다
- **마을 건물 171채** — 다섯 바이옴의 집·밭·작업장·마을 중심부 168채에 성벽·성문·초소. 아이템은 하나고, 무엇의 도면인지는 데이터 컴포넌트다
- **건축가** — 영주를 대신한다. 도면을 등급별로 팔고, **자기 바이옴의 건물만** 그린다
- **촌장이 종을 판다** — 바닐라에서 조합할 수 없는 물건이다
- **전용 크리에이티브 탭**

### 없어진 것

- **격자 도시계획·단계·자동 건축.** 마을 등록, 클레임, `/syvillage` 명령어, 저장되는 마을 데이터가 전부 사라졌다 — **이 모드는 이제 영속 상태를 갖지 않는다**
- **가로등 자동 배치.** 빛은 전부 플레이어가 놓는다. "어디가 어두운지 보여주는" 도구를 대신 넣었다가 그것도 뺐다 — 밤에 제 마을에 서 있으면 어두운 데는 눈에 보인다
- **지면 높이를 읽는 코드 전부.** 바닥은 클릭한 면이고, 자리가 비었는지는 바닐라가 답한다
- **영주의 인장·골렘의 심장.** 값이 아니라 허가를 파는 토큰이었다. 수호상은 건축가가 5등급에 판다
- 서버 config 키가 16개에서 5개로. 읽는 사람 없는 키는 플레이어가 편집하는 파일에서 거짓말이다. 표시 취향 둘(실루엣·표시 진하기)은 서버가 정할 일이 아니라 `syvillage-client.toml`로 나갔다

> **0.2의 config 파일이 남아 있으면 지우고 시작하세요.** 이미 있는 `syvillage-common.toml`은 새 기본값을 받지 않습니다 — 삭제된 키 11개는 무시되지만, 살아남은 키는 옛 값을 그대로 씁니다.

## 0.2.0

- **이름이 바뀌었다: Placitum → SY Village** (modid `placitum` → `syvillage`, 명령어 `/placitum` → `/syvillage`)
- **0.1.x 월드와 호환되지 않는다.** 블록·아이템·직업·마을 데이터가 전부 새 id로 옮겨갔고 옛 id를 읽지 않는다. 0.1.x로 시작한 월드는 탁자·수호상이 사라지고 등록된 마을이 잊힌다 — 새 월드에서 시작할 것

## 0.1.1

- 부지에는 **침대 있는 집과 밀밭만** 선다, 집 셋에 밭 하나꼴. 0.1.0의 축사·대장간·무두장이·퇴비통 농장(11채)은 뺐다 — 실제로 플레이하니 침대 없는 건물이 선 부지는 마을이 자라지 않는 부지였다. 이미 선 것은 그대로 남는다

## 0.1.0

첫 릴리스.

- 종에 시프트+우클릭으로 마을 등록. 등록되지 않은 마을은 코드를 타지 않는다
- 세 단계: 빛(가로등) → 성장(도로·집·밭, 촌장의 탁자) → 방어(성벽·성문·초소, 영주의 탁자)
- 촌장의 탁자, 영주의 탁자 — 주민이 잡는 작업대 둘. 영주의 탁자는 촌장이 파는 **영주의 인장**이 있어야 만든다
- 수호상 — 철골렘 하나를 세우고 죽으면 다시 세운다. 영주가 파는 **골렘의 심장**이 있어야 만든다
- 자유민의 증서 — 촌장이 판다(3등급, 에메랄드 40, 2회). 땅에 쓰면 주민 하나. 두 번째 마을을 열 때 데려가는 가족
- 도시계획은 종 위치의 산술(주기 20). 물·얼음·절벽에서 길이 끊기고, 가파른 부지는 플레이어가 평탄화할 때까지 기다린다
- 재료는 바이옴이 정한다 (평원·타이가·설원·사바나·사막). 길은 바닐라 마을 그대로 흙길, 사막만 사암
- 가로등은 담장 + 울타리 + 랜턴 — 돌벽돌 담장에 그 나무의 울타리, 사막은 사암 담장에 자작나무 울타리
- 성문·초소는 구조물 블록으로 저장한 템플릿. 성벽은 지형을 따라 한 칸씩 오르내린다
- `/syvillage build` — 아무것도 안 지을 때 왜 안 짓는지
- 설정 키 16개, 전부 실제로 읽힌다
