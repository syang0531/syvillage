# SY Village

마인크래프트 자바 에디션 모드. **마을에 대해 플레이어가 할 수 없는 것만 준다.**

> 옛 이름은 *Placitum* — 카롤링거 시대의 공개 집회, 영주와 자유민이 모여 안건을 다루던 자리. 0.2.0에서 Stan Yang의 모드 가족(`sy…`)에 맞춰 **SY Village**(modid `syvillage`)가 됐다. 자유민의 증서는 그 이름의 흔적이다.

**컨셉의 근거와 전체 설계는 `docs/concept-0.3.md`.** 이 문서는 그것의 작업 지침이다.

## 해결하려는 문제

**밤에 몬스터가 주민을 죽인다.** 그리고 **마을을 키우는 일이 너무 오래 걸린다.**

첫째는 처음부터의 문제다. 답은 **빛**이다 — 스폰을 정하는 것은 밝기다. 0.1~0.2는 이 답까지는 맞게 갔는데, 빛을 **우리가 깔았다.** 격자로 깔았고, 목적의 다섯 배 밀도였고, 플레이어가 고를 수 없었다.

둘째는 0.2.0을 실제로 플레이해서 나왔다. 격자가 지형과 안 맞아서 아무것도 안 서거나, 서게 하려고 평탄화하다가 시간이 다 갔다.

**답은 둘 다 같다. 우리가 하지 않는다.**

```
전:  밤에 몹이 주민을 죽인다  →  모드가 빛을 깐다
후:  밤에 몹이 주민을 죽인다  →  모드가 어디가 위험한지 보여준다  →  플레이어가 밝힌다
```

플레이어는 **블록광을 볼 수 없다.** 그게 우리가 줄 수 있는 것이고, 랜턴 놓기는 아니다.

## 환경

| 항목 | 값 |
|---|---|
| Minecraft | 26.2 (Java Edition) |
| 모드로더 | NeoForge 26.2.0.88 |
| Java | 25 |
| modid | `syvillage` |
| 패키지 루트 | `com.syang.syvillage` |
| 빌드 | `./gradlew build` / 실행 `./gradlew runClient` |
| 배포 | CurseForge. `PUBLISHING.md` |

> **26.2 개명:** `ResourceLocation` → `Identifier`, `DimensionDataStorage` → `SavedDataStorage`, `Villager`는 `npc.villager` 패키지, 침대는 `Blocks.BED.pick(DyeColor)`. `RecordCodecBuilder.group()`은 **16필드 상한**.

## 무엇이 들어오는가 — 세 관문

기능을 넣을지 말지는 이 셋이 정한다. **전부 통과해야 들어온다.**

1. **손으로 못 하는가?** (삽이면 되는 길 → 탈락)
2. **마을에 관한 것인가?** (범용 건축 도구 → 탈락)
3. **플레이어가 결정하는가?** (자동 → 탈락)

통과한 것은 넷이다.

| 요소 | 왜 손으로 못 하나 |
|---|---|
| **도면** | 바닐라 마을 건물 152채를 바이옴 재료로, 지형에 맞춰 기단까지 |
| **어둠 표시** | 블록광은 보이지 않는다. 이건 정보다 |
| **수호상** | 골렘을 영구히 붙들어 둔다. 바닐라 골렘은 주민의 기억에 묶여 있다 |
| **개척 키트** | 마을 지도 · **종**(조합할 수 없다) · 자유민의 증서 |

탈락한 것: 도로 · 가로등 배치 · 밭 · 자동 부지 선택 · 단계 · 성벽 링 · 클레임 · 마을 등록 · 자원 · 인구.

## 불변 원칙

### 1. 바닐라를 건드리지 않는다

직업은 작업대가 정한다. 번식은 침대와 음식이 정한다. 죽음은 몹이 정한다. **그중 무엇도 대신 계산하지 않는다.**

주민 기록이 없다. 주민 상태를 엔티티에 저장하지도, 엔티티에서 읽어오지도 않는다. **주민은 그냥 바닐라 주민이다.**

**진행도 바닐라가 잡는다.** 도면은 건축가의 거래 등급으로 풀린다 — 등급 오른 주민은 내려가지 않으므로 우리가 래칫을 만들 필요가 없다. 놓은 종은 바닐라 회합 지점 POI가 되어 주민·골렘 행동에 바로 반영된다. 어둠을 측량할 범위도 바닐라 POI(침대·작업대·회합 지점)가 정한다. **"마을이 어디까지인가"를 우리가 선언하지 않고 묻는다.**

### 2. 플레이어가 정하고, 우리가 짓는다

**자동으로 짓는 것은 없다.** 주기적으로 도는 계획 루프가 없다. 마을을 훑는 순회가 없다.

도면을 놓는 것도, 어디에 놓을지도, 얼마나 높이 앉힐지도, 랜턴을 어디에 걸지도 플레이어다. 우리가 하는 일은 **그 결정을 블록으로 옮기는 것**과 **그가 볼 수 없는 것을 보여주는 것**뿐이다.

자원도 재고도 없다. **에메랄드가 비용이다** — 도면 값. "건설이 시간을 쓰는 것으로 충분하다"는 가짜 비용이었다. 플레이어는 그냥 기다렸고, 기다리려면 청크를 붙들어야 했다.

### 3. 이 모드는 영속 상태를 갖지 않는다

`SavedData`가 없다. `SettlementManager`가 없다. 등록도 클레임도 없다.

| 무엇 | 어디에 |
|---|---|
| 도면 | 아이템 |
| 거래 | `data/syvillage/trade_set` (바닐라 데이터) |
| 템플릿 | 정적 리소스 (`.nbt`) |
| 수호상의 기억 | 블록 엔티티 NBT (바닐라 기계) |
| 제도판의 도면·방향·좌표 | **블록 엔티티 NBT** (바닐라 기계) |
| 윤곽 | 블록 엔티티가 계산해 자기 동기화에 실어 보낸다 |
| 건축 연출 | 메모리, 끊기면 즉시 완료 |

`docs/why-the-reset.md`의 "아무 일도 안 일어난다" 여덟 번은 **전부 생애주기가 있는 상태**에서 나왔다. 큐가 뒤섞이고, 완료된 작업이 버려지고, 재개할 것이 없었다. **그 범주를 통째로 없앤다.**

> **시금석: 종을 옮겨서 깨지는 것이 있으면, 우리가 뭔가를 저장하고 있다는 뜻이다.**

블록 엔티티는 예외가 아니라 **같은 규칙의 반대편**이다. 부수면 사라지고, 월드가 관리하고, 우리가 만든 생애주기가 없다. 수호상의 기억이 먼저 그 자리에 있었다.

건축 연출(몇 초에 걸쳐 아래에서 위로)은 **큐가 아니다.** 블록은 즉시 확정하고 보이는 것만 늦춘다. 서버가 꺼지거나 청크가 내려가면 남은 것을 그 자리에서 완료한다. 최악이 "건물이 즉시 완성됨"이라 복구 로직 자체가 필요 없다.

### 4. `expand`는 순수 함수다

```
expand(도면, 위치, 회전, 얼린 지면) → 블록 목록
```

지면은 확정 시점에 한 번 읽어 박제한다. 같은 입력은 항상 같은 블록 목록을 낸다 — **그래야 테스트할 수 있다.**

### 5. 좌표는 플레이어가 정한다

> **당신이 클릭한 면이 바닥이다.**

탐색하지 않는다. 계산하지도 않는다. **묻지도 않는다 — 이미 말했으니까.**

**도면은 손에서 아무것도 하지 않는다. 건축가의 탁자에 꽂는다.**

| 동작 | 결과 |
|---|---|
| 도면 들고 탁자 우클릭 | 꽂히면서 제도판이 열린다 |
| 빈손으로 탁자 우클릭 | 제도판 |
| 제도판의 X/Y/Z | 탁자 기준 **상대 좌표**. 치는 대로 건물이 걸어간다 |
| 방향 / 윤곽 / 도면 꺼내기 / 짓기 | 버튼 넷 |

**윤곽은 탁자가 서 있는 동안 켜져 있다.** 타이머가 없다 — 그게 이 구조의 전부다. 둘러보고 오든, 막는 블록을 캐러 갔다 오든, 돌아오면 그대로 있다. 동시에 둘을 놓아보려면 **탁자를 둘 짓는다**: 숫자가 아니라 물건이 한계다.

**템플릿 맨 아래층이 클릭한 면에 앉는다.** 블록 하나를 놓는 것과 똑같이. `Template.lift()`는 산술에서 빠지고 미리보기 정보로만 남는다 — 끼우면 템플릿마다 한 칸씩 파묻혀서 "클릭한 면이 바닥"이 거짓이 된다. *"출입구끼리 높이가 달라서 기다린다"* 는 규칙은 사라졌다: 그 규칙이 있었던 이유는 **물어볼 사람이 없었기 때문**이다.

**윤곽은 서버가 재고 클라이언트가 그린다.** 기하만 패킷으로 건너간다(`PreviewOutline`) — 템플릿도 팔레트도 판정도 안 간다. 패킷은 윤곽이 뜰 때와 사라질 때 한 번씩이고, 그동안 클라이언트가 제가 가진 것을 매 틱 다시 제출한다.

**건물의 바닥은 탁자의 높이다**(오프셋 Y를 더한). 어느 면을 클릭했느냐가 사라진다.

**윤곽은 셋이다.** 하얀 육면체(상자의 크기) · 초록/빨강 테두리(블록이 서는 칸) · **반투명 부피**(기둥마다 바닥~꼭대기 막대). 셋째가 없으면 **그 건물을 본 적 없는 사람은 뭐가 지어지는지 모른다** — 바닥 테두리는 아는 사람에게만 말을 건다. 기둥 막대는 221개, 블록 단위 고스트는 1209개고, 실루엣이 답을 나른다.

**화면이 하나 있다.** 안 두려고 했고, "같은 블록을 30초 안에 두 번"이 그 대가였다. 손에 든 게 아니라 **골짜기 건너에 놓인 건물**을 배치하는 일이라, 버튼 넷과 숫자 셋이 몸짓도 규칙도 더 적다. 구조 블록의 화면이 선례다.

성벽·성문·초소도 도면이다. 단위는 **9** — 초소 본체가 9×9라 저절로 맞는다. 이어 놓으면 직전 조각의 끝 높이를 입력으로 받아 회전과 높이가 붙는다.

### 6. 자리가 차 있으면 놓지 않는다 — 침대와 같은 규칙

**차지할 자리가 전부 비어 있어야 한다.** 아니면 거절한다. 그게 전부다.

바닐라가 블록을 놓는 규칙 그대로다. 침대는 어느 쪽 칸이든 막혀 있으면 안 놓이고, 이건 이천 칸이 그렇다.

- **채우지 않는다.** 기단도, 기초도 없다. 밑을 비워두는 것이 플레이어의 의도일 수 있다
- **깎지 않는다.** 언덕이 발자국에 들어오면 거절이다
- **베지 않는다.** 나무가 서 있으면 거절이다
- **덮어쓰지 않는다.** 남의 집에 겹치면 거절이다

넷 다 **미리보기가 어느 칸인지 보여주므로 잡일 지시가 아니라 정보다.** 치울지 다른 데 놓을지는 플레이어가 정한다. `Clearance`와 `Spans`가 이 규칙 때문에 없어졌다.

**지지대는 묻지 않는다.** 침대 머리맡이 절벽 밖으로 나가도 놓이는 것과 같다. 허공에 뜨는 기둥은 세어서 **알려주고, 거절하지 않는다.**

> 차지하는 것은 **상자가 아니라 기둥별 구간**이다 — 그 기둥에서 템플릿이 쓰는 가장 낮은 층부터 가장 높은 층까지, 사이의 공기까지. 벽은 비켜 갔는데 거실 한가운데 흙더미가 남는 것을 막는다. 템플릿이 아무것도 안 쓰는 기둥(성문 아치 밑)은 아예 안 본다.

### 7. 재료는 **놓인 자리의** 바이옴이 정한다

`Craft`는 사다리가 아니라 **팔레트**다. 사막에 놓은 성문은 사암이 된다. 마을이 아니라 그 자리의 바이옴을 읽는다 — 도면은 어디에나 놓을 수 있으니까.

사다리였을 때는 꼭대기에서 도로·집·성벽이 전부 돌벽돌이 되어 마을이 삭막해졌다. 팔레트는 **기단과 성벽·성문·초소만** 담당한다. 바닐라 집 템플릿은 제 재료를 들고 온다.

**팔레트는 확정 시점에 박제된다.** `expand`가 순수 함수로 남는 것은 덤이 아니라 이것 때문이다.

### 8. **우리가 지은 것이 지형 읽기를 바꾼다**

이 프로젝트에서 같은 버그가 **다섯 번** 났다. 전부 같은 모양이다.

- 벽의 "이미 서 있나" 검사가 **하늘을 봤다**
- 벽이 **자기를 가뒀다**
- 계단이 **벽 꼭대기에 맞춰** 설 뻔했다
- 평탄도 검사가 **자기가 지은 벽을 비탈로 읽었다**
- 성문·초소가 **제 지붕 위에 한 층을 더 올렸다**

`groundAt`은 **우리 석재 꼭대기를 지면으로 돌려준다.** 우리 것과 줄을 맞춰야 하는 계산 — 성벽 조각을 **직전 조각에 이어 붙이는 것**이 그 자리다 — 은 `Terrain.footingAt`으로 **우리 석재를 뚫고 내려가** 진짜 땅을 읽어야 한다.

그리고 `footingAt`은 **우리 석재가 끊기는 지점에서 멈춘다.** 성문 아치처럼 구조물에 구멍이 있으면 그 칸은 땅을 못 찾는다 — **바닥을 정하는 계산은 속이 막힌 기둥만 써야 한다.**

### 9. 아무것도 안 할 때는 왜 안 하는지 말한다

단위는 **도면 한 장**이다. 부지 집계가 아니라, 우클릭 한 번에 **한 줄**.

> *"블록 23칸이 자리를 차지합니다. 첫 자리는 −118, 71, 402"* / *"가장 깊은 곳이 땅에서 6칸 떠 있습니다"* / *"스폰 가능한 자리 37곳"*

> **개수가 아니라 깊이인 이유:** 처음엔 밑이 빈 기둥을 셌는데, 자연 지형에서 매번 221 중 220이 나왔다. 숫자는 맞는데 아무 말도 안 한다 — 바닥은 평면 하나고 땅은 아니니까. **행동으로 이어지지 않는 참인 숫자는 원칙 9를 만족하지 않는다.**

boolean을 돌려주는 함수를 새로 만들지 말 것. **미리보기가 이 모드의 UI 전부다** — undo도 GUI도 없으므로 여기가 무너지면 *"1칸 틀려서 부수고 다시"* 가 그대로 돌아온다.

### 10. 모양은 플레이어가 짓고, 코드는 자리만 안다

성문·초소·성벽·수호상은 **템플릿**이다 — 크리에이티브에서 손으로 짓고 구조물 블록으로 저장한 `.nbt`가 `data/syvillage/structure/`에 들어 있다. 집은 바닐라 것을 읽는다. `Template`이 게임 없이 읽고, `TemplatePlan`이 어디에 놓을지·섰는지·무슨 블록인지를 한 곳에서 정한다.

규칙으로 짓던 시절의 버그는 전부 **같은 모양을 세 곳에서 세 가지로 읽어서** 났다. 템플릿은 한 가지로만 읽힌다. 모양을 바꾸고 싶으면 **코드가 아니라 월드에서** 바꾼다 — `tools/dump_structure.py`로 층별로 확인할 수 있다.

발자국은 **상자가 아니라 블록이 있는 기둥**이다.

## 절대 금지

- **`SavedData`를 다시 만들기** — 원칙 3. 저장하고 싶은 것이 생기면 그건 설계가 틀린 것이다
- **주기적으로 도는 계획 루프를 두기** — 원칙 2. 모든 것은 플레이어의 상호작용에서 시작한다

  > **연출 틱은 다르다.** 건물이 올라가는 것이고, 판별법은 넷이다: 플레이어가 몇 초 전에 시작한 것만큼만 돌고 · 월드를 **안 읽고** · 저장되는 것이 **없고** · 끊기면 재개가 아니라 **즉시 완료**한다. 계획 틱은 넷 다 아니었고, 성벽 파이프라인 버그 여섯이 그 위에 살았다.

  > **제도판의 재측량도 다르다.** 이건 월드를 읽는다 — 그래서 조건이 더 빡빡하다: **플레이어가 지은 탁자**만 · **윤곽이 켜진** 것만 · **근처에 사람이 있을 때만** · 그리고 **아무것도 정하지 않는다.** 이미 화면에 떠 있는 답을 다시 읽을 뿐이다. 이게 있어야 막는 블록을 캐고 왔을 때 빨강이 초록이 된다. 금지된 루프는 아무도 요청하지 않은 일을 찾아 클레임 전체를 걸어다녔다.
- **자동으로 뭔가를 지어주기** — 세 관문의 3번
- 주민을 시뮬레이션하기 (인구·식량·사기·직업·나이 — 전부 한 번 해봤고 실패했다)
- **같은 것을 정하는 config 키를 둘 두기** — 간격과 배치 크기를 따로 뒀더니 예전 월드 config가 한쪽을 되돌려 10배 가속이 정확히 상쇄됐다
- **읽는 사람이 없는 config 키를 남기기** — 키는 "돌리면 게임이 바뀐다"는 약속이다. 70개 중 56개가 삭제된 시뮬레이션의 잔해였고, 플레이어가 편집하는 파일에서 그건 지저분함이 아니라 거짓말이다
- 하드코딩된 밸런스 상수 → 전부 config로
- `HashMap` 순회 순서에 의존
- **클라이언트에 판정을 두기** — `client/`는 **그리기만** 한다. 무엇을 지을 수 있는지는 전부 서버가 정하고, 모드 없는 클라이언트는 윤곽만 못 볼 뿐 도면은 그대로 동작해야 한다

  > **파티클 금지는 풀렸다.** *"파티클과 채팅으로 안 되는 것이 확인된 다음에"* 가 조건이었고, 게임에서 확인됐다 — 가루는 선이 안 된다. 멀어지면 흩어지고 굵기를 못 정하고, 발자국을 덮으면 판단할 지면을 가리는 초록 안개가 된다. 26.2의 **Gizmo API**(`Gizmos.cuboid` · `Gizmos.line` · `GizmoStyle`)가 구조 블록이 쓰는 그 선이다

## 코드 규약

- 상태 객체는 **불변 record + Codec**
- 식별자·주석·커밋 메시지는 영어. 설계 문서는 한국어
- 밸런스 숫자는 전부 `SyVillageConfig`로
- 로깅은 `SyVillage.LOGGER`

## 패키지 구조

```
com.syang.syvillage
├─ SyVillage.java      @Mod 진입점
├─ block/             블록과 블록 엔티티 (수호상, 두 탁자)
├─ item/              아이템 (도면, 자유민의 증서)
├─ data/              record + Codec (BuildOp, Craft)
├─ build/             템플릿, 자리 판정, 미리보기, 배치 / 지형 읽기와 걷기
├─ net/               제도판 버튼 패킷 하나 (클라이언트 → 서버)
├─ client/            윤곽 그리기(Gizmo)와 제도판 화면. **판정하지 않는다**
├─ event/             연출 틱
└─ config/            SyVillageConfig
```

`store/`와 `settlement/`는 **없다** — 저장할 것도 등록할 것도 없다.

`command/`는 아직 없다. 격자를 걷어낼 때 안에 있던 것이 전부 등록·부지·큐라 통째로 지웠고, 필요해지는 단계에서 그만큼만 돌아온다 — **빈 채로 남겨두지 않는다.**

## 작업 방식

- 컨셉은 `docs/concept-0.3.md`. 의문이 생기면 코드를 먼저 쓰지 말고 질문할 것
- `docs/design.md`와 `docs/roadmap.md`는 **기록이지 계획이 아니다.** 격자·단계·자동 건축 서술은 폐기됐다. `docs/why-the-reset.md`는 그대로 유효하다
- **이 프로젝트에서 가장 비싸게 배운 것:** "아무 일도 안 일어난다"는 관측에는 늘 설명이 둘 이상 있다. 도구가 자기가 **무엇을 안 했는지** 말하게 만들 것. 여덟 번 반복됐다
- **그건 도구를 쓸 때도 똑같다.** 아무것도 못 찾은 문자열 치환은 아무 말도 하지 않는다. 그렇게 "고쳤다"고 커밋된 물 판정이 한 번도 발화한 적이 없었다. 편집은 **일치를 검증**할 것
