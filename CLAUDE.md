# Placitum

마인크래프트 자바 에디션 모드. **종으로 지정한 마을이 스스로 인프라를 짓는다.**

> *placitum* — 카롤링거 시대의 공개 집회. 영주와 자유민이 모여 안건을 다루던 자리.

## 해결하려는 문제

**밤에 몬스터가 주민을 죽인다.**

이게 처음부터 유일한 문제였다. 이전 설계는 이걸 습격 시뮬레이션으로 옮겨서 풀었고, 그동안 실제 몹은 성벽 안에서 계속 스폰됐다. 갈아엎은 이유는 `docs/why-the-reset.md`.

답은 **빛**이다. 스폰을 정하는 것은 밝기다. 벽은 빛을 만들지 않으므로 짓지 않는다 — 고정된 링이라 마을이 커지면 틀린 자리에 남기도 한다.

## 환경

| 항목 | 값 |
|---|---|
| Minecraft | 26.2 (Java Edition) |
| 모드로더 | NeoForge 26.2.0.88 |
| Java | 25 |
| modid | `placitum` |
| 패키지 루트 | `com.syang.placitum` |
| 빌드 | `./gradlew build` / 실행 `./gradlew runClient` |
| 배포 | CurseForge. `PUBLISHING.md` |

> **26.2 개명:** `ResourceLocation` → `Identifier`, `DimensionDataStorage` → `SavedDataStorage`, `Villager`는 `npc.villager` 패키지, 침대는 `Blocks.BED.pick(DyeColor)`. `RecordCodecBuilder.group()`은 **16필드 상한**.

## 불변 원칙

### 1. 바닐라를 건드리지 않는다

직업은 작업대가 정한다. 번식은 침대와 음식이 정한다. 죽음은 몹이 정한다. **그중 무엇도 대신 계산하지 않는다.**

주민 기록이 없다. 주민 상태를 엔티티에 저장하지도, 엔티티에서 읽어오지도 않는다. **주민은 그냥 바닐라 주민이다.**

### 2. 우리가 하는 일은 건물을 짓는 것뿐이다

마을이 **무엇을 해도 되는지**는 `Settlement.stage`가 정한다 — 셋 다 동사다:

| 단계 | 열쇠 | 마을이 하는 일 |
|---|---|---|
| `LIT` | 종 | 가로등만. 모드의 약속은 여기서 이미 다 지켜진다 |
| `HEADED` | 촌장 탁자를 누가 잡음 | 도로, 집, 밭 |
| `WALLED` | 영주 탁자를 누가 잡음 | 성벽·성문·초소 (계단은 성문·초소 안에) |

도로가 촌장 뒤에 있는 이유는 지형이 아니다 — **도로는 건축이고 빛은 설비다.** 격자로 깔린 길은 "누가 이 마을을 정리하고 있다"는 유일한 시각적 증거고, 몹은 촌장이 있는지 묻지 않는다. 지면 걷기의 씨앗은 깔린 포장이 아니라 걷기 결과라서, 도로 없이도 가로등은 선다.

**단계(phase)** 단위로 짓는다. 단계는 도로로 둘러싸인 블록의 테두리다 — 단계 0은 종에서 만나는 네 블록(부지 16개), 단계 1은 그 바깥 12블록, 단계 2는 20블록.

```
각 단계 안에서:  도로  →  가로등  →  집 / 밭     (LIT에서는 가로등만)
```

**그 단계에 더 할 게 없어야 다음 단계를 본다.** 매번 0부터 다시 훑으므로, 플레이어가 안쪽 부지를 평탄화하면 거기부터 다시 짓는다.

마지막 단계는 **도로와 가로등만, 바깥이 열린 ㄷ 모양**이다. 끝은 **클레임**이 정하고 `buildMaxPhases`는 천장일 뿐.

무엇을 지을지는 **월드에 묻는다** — 침대 ≤ 주민이면 집, 아니면 밭. 둘 다 바닐라가 이미 세고 있는 숫자다.

자원도 재고도 비용도 없다. **건설이 시간을 쓰는 것으로 충분하다.**

### 3. 청크가 로드된 동안에만 짓는다

LOD 없음. promote/demote 없음. replay 없음. 안 가본 마을은 자라지 않는다.

### 4. `expand(recipe)`는 순수 함수다

지면은 계획 단계에서 한 번 읽어 `groundProfile`과 `Spans`에 박제한다. 같은 recipe는 항상 같은 블록 목록을 낸다 — **그래야 테스트할 수 있다.**

### 5. 좌표는 계획이 정한다. 탐색하지 않는다

`TownPlan`의 모든 좌표는 **종 위치의 함수**다. 주기 20 = 도로 3 + (여유 1 + 부지 7) × 2 + 여유 1. 도로로 둘러싸인 블록 하나에 **부지 4개, 가로등 5개**(꼭지점 4 + 중앙 1). `CellPos`는 주기가 아니라 **부지 하나**를 가리킨다.

건축물도 밭도 **부지 7×7 전체**를 쓴다 — 바닐라 마을 건물은 지붕까지 7×7이다. 집 문은 남/북이고 **반드시 도로 쪽** — 블록의 어느 절반에 있느냐는 사실이지 판단이 아니다. 어느 쪽이 앞인지는 템플릿의 `building_entrance` 직소가 말한다.

부지 선택을 탐색으로 했을 때 나온 버그는 전부 같은 모양이었다 — 한 조건에는 맞고 다른 조건에는 틀린 자리.

### 6. 이미 서 있는 것을 파괴하지 않는다

건설 전에 월드에 묻는다(`GridSurvey.builtOn`). 지형도 깎지 않는다 — 부지가 평평하지 않으면 그냥 기다린다.

예외는 **나무**다. 지면 읽기가 통나무를 일부러 지나쳐 내려가므로(나무 한 그루가 부지를 거부하면 숲 마을은 441칸 중 159칸이 막힌다), 그 규칙의 나머지 절반으로 **짓기 직전에 벤다**(`Clearance`).

### 7. 단계는 **직업**이 열고, 재료는 **바이옴**이 정한다

단계는 주민의 직업에서 읽는다 — 우리 직업 둘뿐이다. 인구가 아니다. 인구는 **기다리는** 것이고, 직업은 플레이어가 **작업대를 놓는** 것이다. 그리고 블록이 놓인 게 아니라 **직업이 채워져야** 한다: 촌장이 생기려면 놀고 있는 주민이 있어야 하고, 그게 진짜 성장의 신호다. 바닐라 석공은 이 모드에서 아무 의미도 없다.

**내려가지 않는다** — `Settlement.stage`가 최고 수위를 기록한다. 살아있는 마을에서 읽는 값은 뭐든 사라질 수 있으므로, 래칫은 읽는 쪽이 아니라 저장된 쪽에 있다.

**재료(`Craft`)는 사다리가 아니라 팔레트다.** 등록할 때 종 자리의 바이옴으로 한 번 정하고 다시 바꾸지 않는다. 사다리였을 때는 꼭대기에서 도로·집·성벽이 전부 돌벽돌이 되어 마을이 삭막해졌다. 팔레트는 **도로와 성벽만** 담당하고, 옛 사다리의 세 값(`timber`/`stone`/`masonry`)은 세이브와 큐에 남은 recipe가 부르기 때문에 읽기 전용으로 남아 있다.

**업그레이드를 아는 코드가 없다.** 도로 플래너는 "이게 어느 팔레트든 우리 포장재인가"만 묻고, 그러면 놔둔다. 집은 플레이어가 부수면 부지 기록이 지워지고 다시 선다 — 남의 벽을 덮어쓰는 규칙이 필요 없다.

**두 탁자는 거래로 잠겨 있다.** 영주의 탁자에는 촌장이 파는 **영주의 인장**이, 수호상에는 영주가 파는 **골렘의 심장**이 든다. 우리가 세는 것이 아니라 바닐라 거래(`trade_set`)가 하는 일이다. 종은 잠그지 않는다 — 잠그면 `종 → 촌장 → 영주 → 종`으로 순환하고, 작은 마을로 남기고 싶은 사람에게 제약을 줄 이유도 없다.

### 8. 걸어갈 수 없는 곳에는 아무것도 짓지 않는다

`Reach`가 걷는다 — 물이 아니고 높이차 1 이하일 때만 이웃 기둥으로. 얼음은 물이고(랜턴이 녹인다), 가루눈은 뚜껑 덮인 구멍이고, 용암은 유체다 — 전부 `Ground.unfit`이 땅이 아니라고 답한다. 걷기는 **둘**이다.

- **도로 걷기**는 도로선 위로만 번진다. 그래서 길이 물가와 절벽에서 끊기고 건너편에 다시 안 나타난다. 기복이 `maxRoadClimb`를 넘어도 끊기고, **아무 데도 도착하지 못한 오르막은 걷기가 끝난 뒤 잘라낸다**(경사로 토막 금지)
- **지면 걷기**는 포장된 도로에서 번진다. 가로등과 부지가 쓴다

가로등은 도로보다 `lampBlocksBeyondStreet`만큼 더 간다. **빛은 도로가 아니다** — 길이 언덕에서 포기해도 몹은 안 한다.

### 9. 이 모드는 플레이어와 **같이** 짓는다

일부러 할 일을 남긴다. 부지가 가파르면 짓지 않고 멈추고, **플레이어가 평지를 만들어주는 것이 의도된 고리다.** 재미는 거기에 있지, 모드가 혼자 완성된 마을을 뽑아내는 데 있지 않다.

**도로는 부수면 다시 깔고, 가로등은 부수면 그대로 둔다.** 마을이 세운 가로등 자리는 `Settlement.lamps`에 기록되고, 기록된 자리는 두 번 세우지 않는다 — 어두운 것은 플레이어의 선택이다. 도로는 그런 기록이 없어서 빈 자리를 다시 채운다.

**성문·초소의 바닥은 출입구가 정한다.** 템플릿 맨 아래층의 계단·울타리·울타리문이 서는 땅. 나머지 기둥은 그보다 한 칸 높기까지는 덮고, 더 높으면 짓지 않고 기다린다 — 낮은 땅은 기초로 채우면 되지만 높은 땅은 깎아야 하고, 우리는 깎지 않는다. 성벽은 지형을 따라 한 칸씩 오르내리되 **같은 높이가 두 단면은 이어져야** 다음 단이 있다.

그러므로 `steep`/`unreachable`이 많은 것은 **버그가 아니다.** 건축률을 올리려고 `maxCellSlope`를 풀거나 지형을 깎는 기능을 넣지 말 것. 규칙은 엄격하게 두고, 대신 **거절의 이유를 읽을 수 있게** 유지한다.

### 10. 계획이 요청한 것은 전부 블록이 되어야 한다

`plan`이 큐에 넣은 기둥이 `expand`에서 아무 블록도 안 내면, **정주지는 그것을 영원히 다시 요청한다.** 성문 자리를 `expand`에서 건너뛰었더니 같은 32기둥을 1초마다 무한히 계획했다.

규칙은 **둘 중 한쪽에만** 있어야 한다. 짓지 않을 것이면 `plan`에서 거르고, `expand`는 받은 것을 전부 짓는다.

**`expand`가 자기가 놓은 블록을 지워도 같은 일이 일어난다.** 벌목(`Clearance.ops`)에 **자기가 쓴 자리를 전부** 넘겨야 한다 — 빈 집합을 넘겼더니 계단이 지어지자마자 벌목에 지워졌고, 817번 다시 지었다. `expand`의 결과에 같은 좌표가 두 번 나오면 그게 신호다.

### 11. **우리가 지은 것이 지형 읽기를 바꾼다**

이 프로젝트에서 같은 버그가 **네 번** 났다. 전부 같은 모양이다.

- 벽의 "이미 서 있나" 검사가 **하늘을 봤다** (벽이 지면을 자기 높이만큼 올려서)
- 벽이 **자기를 가뒀다** (다 쌓은 안쪽 면을 걷기가 못 넘어서)
- 계단이 **벽 꼭대기에 맞춰 4칸 높이** 설 뻔했다
- 평탄도 검사가 **자기가 지은 벽을 비탈로 읽어서** 벽이 평행한 줄무늬로 나왔다
- 성문·초소가 **제 지붕 위에 한 층을 더 올렸다** (다 지은 구조물의 지면이 8칸 위로 읽혀서, 바닥을 거기 맞추고 데크를 다시 그 위에서 찾았다 — 하늘에서)

`groundAt`은 **우리 석재 꼭대기를 지면으로 돌려준다.** 우리 것과 줄을 맞춰야 하는 계산은 `GridSurvey.footingAt`으로 **우리 석재를 뚫고 내려가** 진짜 땅을 읽어야 한다.

도로가 이 함정에 안 빠지는 이유는 포장이 **제자리 교체**라서다. 그래서 도로의 검사를 베끼면 틀린다.

그리고 `footingAt`은 **우리 석재가 끊기는 지점에서 멈춘다.** 성문 아치처럼 구조물에 구멍이 있으면 그 칸은 땅을 못 찾고 끝난다 — **바닥을 정하는 계산은 속이 막힌 기둥만 써야 한다.**

### 12. 아무것도 안 할 때는 왜 안 하는지 말한다

`Lots.Verdict`가 부지마다 이유를 하나 낸다. boolean을 돌려주는 함수를 새로 만들지 말 것. 유휴 마을은 30초마다 집계를 로그에 남기고, `/placitum build`와 종 시프트+우클릭이 같은 것을 보여준다.

## 절대 금지

- 주민을 시뮬레이션하기 (인구·식량·사기·직업·나이 — 전부 한 번 해봤고 실패했다)
- 매 틱 전체 마을 순회
- 클레임 전체 범위 AABB 엔티티 스캔을 **매 틱** 하기 (지을 자리를 찾은 뒤에 한 번만)
- **같은 것을 정하는 config 키를 둘 두기** — 간격과 배치 크기를 따로 뒀더니 예전 월드 config가 한쪽을 되돌려 10배 가속이 정확히 상쇄됐다
- **읽는 사람이 없는 config 키를 남기기** — 키는 "돌리면 게임이 바뀐다"는 약속이다. 70개 중 56개가 삭제된 시뮬레이션의 잔해였고, 플레이어가 편집하는 파일에서 그건 지저분함이 아니라 거짓말이다
- 하드코딩된 밸런스 상수 → 전부 config로
- `HashMap` 순회 순서에 의존
- 기존 저장 포맷에 **필수** 필드 추가 → `optionalFieldOf` + 기본값

### 13. 모양은 플레이어가 짓고, 코드는 자리만 안다

성문·초소는 **템플릿**이다 — 크리에이티브에서 손으로 짓고 구조물 블록으로 저장한 `.nbt`가 `data/placitum/structure/`에 들어 있다. `Template`이 게임 없이 읽고, `TemplatePlan`이 어디에 놓을지·섰는지·무슨 블록인지를 한 곳에서 정한다. 성벽 단면(폭 5)도 같은 방법으로 받은 샘플을 규칙으로 옮긴 것이다.

규칙으로 짓던 시절의 버그는 전부 **같은 모양을 세 곳에서 세 가지로 읽어서** 났다 (바닥은 엉뚱한 사각형에서, 서 있는지는 안 짓는 칸에서). 템플릿은 한 가지로만 읽힌다. 모양을 바꾸고 싶으면 **코드가 아니라 월드에서** 바꾼다 — `tools/dump_structure.py`로 층별로 확인할 수 있다.

발자국은 **상자가 아니라 블록이 있는 기둥**이다. 저장 상자 안의 빈 땅은 아무도 걸어갈 수 있어야 할 이유가 없다.

## 코드 규약

- 상태 객체는 **불변 record + Codec**
- 식별자·주석·커밋 메시지는 영어. 설계 문서는 한국어
- 밸런스 숫자는 전부 `PlacitumConfig`로
- 로깅은 `Placitum.LOGGER`

## 패키지 구조

```
com.syang.placitum
├─ Placitum.java      @Mod 진입점
├─ block/             블록과 블록 엔티티 (수호상)
├─ data/              record + Codec
├─ store/             SavedData, SettlementManager
├─ build/             측량, 걷기, 계획, 전개, 배치
├─ settlement/        등록, 이름
├─ command/           명령어, 리포트
├─ event/             종 상호작용, 서버 틱
└─ config/            PlacitumConfig
```

## 작업 방식

- 설계는 `docs/design.md`, 명령어·설정은 `docs/commands-and-config.md`. 의문이 생기면 코드를 먼저 쓰지 말고 질문할 것
- **이 프로젝트에서 가장 비싸게 배운 것:** "아무 일도 안 일어난다"는 관측에는 늘 설명이 둘 이상 있다. 도구가 자기가 **무엇을 안 했는지** 말하게 만들 것. 여덟 번 반복됐다 (`docs/why-the-reset.md`)
- **그건 도구를 쓸 때도 똑같다.** 아무것도 못 찾은 문자열 치환은 아무 말도 하지 않는다. 그렇게 "고쳤다"고 커밋된 물 판정이 한 번도 발화한 적이 없었다. 편집은 **일치를 검증**할 것
